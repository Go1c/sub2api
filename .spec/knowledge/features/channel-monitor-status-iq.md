---
name: channel-monitor-status-iq
description: 渠道监控「状态 + 智商」：一次糖果题请求同时记下服务器状态和智商四态；用户页两条时间线
metadata:
  type: doc
  level: L2
  status: 已交付
---

# 渠道监控 · 状态 + 智商

在「新增渠道监控」的检查方式里增加第四项 **状态 + 智商**。用户页仍是两条时间线：上边服务器状态，下边智商状态。两码事：上游可以回复正常，但智商降了。

少打一个请求：不另发算术探活。只发 kin 摸糖果题，用这一次响应同时填两条。

可点 Mock：`.spec/mocks/channel-monitor-status-iq.html`。

## 背景 / 目标

- 现有探活会单独打算术题。开智商后再打一枪浪费，也容易和智商结果对不齐。
- 服务器通不通、快不快，跟题答得对不对，要分开看。
- 目标：一次糖果题，拆出两套状态，画成两条棒。

## 题目（与 kin 默认值相同）

测试题：

> 黑色袋子中有苹果味、桃子味、西瓜味糖果;每种分为圆形和五角星形，可用手感区分形状。圆形依次有7、9、8颗;五角星形依次有7、6、4颗。事先决定摸出的数量，最少取多少颗，才能保证拿到不同形状的苹果味和桃子味糖果?

标准答案：`21`

判定：去空白和常见标点后，模糊匹配为包含标准答案；关闭模糊则为整段相等。与 `TurnStateProbeAnswerMatches` 同一套。

## 一次请求 · 两条时间线

| 这次糖果题发生了什么 | 上边 · 服务器 | 下边 · 智商 |
|----------------------|---------------|-------------|
| 答对 | 正常；慢则降级 | **不降智**（绿） |
| 完整响应但答错 | 正常；慢则降级 | **降智**（橙） |
| 接口错误、超时、空响应、未完整结束 | 失败 | **测试异常**（红） |
| 请求发出前监控侧 DNS 解析失败 | 灰（没打到上游） | **监控网络异常**（灰） |

卡片右上角标跟服务器最新态（和现在一样：正常 / 降级 / 失败）。智商不另做芯片，只靠下边那条棒的颜色：绿不降智、橙降智、红测试异常、灰监控网络异常。

没开智商的渠道，仍只有一条算术探活棒。

## 设计

- **交互面**
  - 检查方式四选一。选智商后填上游、Key、主模型，展开测试题 / 答案 / 模糊匹配。默认 kin 题和 `21`。
  - 用户状态页：开智商后两条「近 60 次」——上服务器、下智商。不展示题目原文。
- **实现面**
  - `check_mode = iq`。只发测试题，不发算术 challenge。
  - 同一轮结果写入服务器时间线（延迟、是否完整响应）和智商时间线（四态）。
  - 不抽 SVG。
  - 附加模型：每个附加模型也打同一道糖果题（与探活一致）。
  - 检测间隔仍为 15–3600 秒，抖动/启用/分组/高级设置保留。
  - 7 天可用率仍按服务器完整响应（operational + degraded）计算。
- **设计面**
  - 不并进 `/admin/channels/iq`。
  - 两条线、一次请求。不是两条线打两枪，也不是一条线混两种意思。

## 已决策

- 题目 / 答案 / 模糊匹配：复用 kin Turn-State 探测，不用鹈鹕题。
- 展示两条时间线；探测只打糖果题。
- 智商文案：不降智 / 降智 / 测试异常 / 监控网络异常。
- 附加模型：每个附加模型也打糖果题。
- IQ 模式间隔下限维持 15s（与探活相同）。
- 7 天可用率按服务器完整响应（operational + degraded）计算。

## 相关

- [channel-iq-detect.md](channel-iq-detect.md)（内部号 SVG 看板，本功能不走这条）
- Mock：`.spec/mocks/channel-monitor-status-iq.html`
- kin 题目与判定：`origin/main-kin` `backend/internal/service/turn_state_probe.go`
- 现有算术探活：`backend/internal/service/channel_monitor_challenge.go`
